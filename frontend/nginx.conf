server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Allow uploading images of a reasonable size
    client_max_body_size 20m;

    # Reverse-proxy API calls to the backend so the browser sees same-origin (no CORS).
    # "app" is the backend service name on the Docker Compose network.
    location /api/ {
        proxy_pass http://app:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Single-page app: serve index.html for client-side routes.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
